#include <iostream>

int main ()
{
    constexpr int ROZMIAR = 20;
    int min = 0;
    int max = 0;
    int tab[ROZMIAR];
    for (int i = 0; i < ROZMIAR; i++)
    {
        tab[i] = rand() % 20;
    }
    cout << tab;
    for (int i = 0; i < ROZMIAR; i++)
    {
        if (i = 0)
    }
}